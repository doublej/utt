<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>CFBundleDevelopmentRegion</key><string>en</string>
  <key>CFBundleExecutable</key><string>$(EXECUTABLE_NAME)</string>
  <key>CFBundleIdentifier</key><string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
  <key>CFBundleName</key><string>$(PRODUCT_NAME)</string>
  <key>CFBundlePackageType</key><string>APPL</string>
  <key>CFBundleShortVersionString</key><string>$(MARKETING_VERSION)</string>
  <key>CFBundleVersion</key><string>$(CURRENT_PROJECT_VERSION)</string>
  <key>LSMinimumSystemVersion</key><string>$(MACOSX_DEPLOYMENT_TARGET)</string>
  <!-- Deliberately *not* LSUIElement. It made every launch start as an accessory,
       and an accessory app is not granted activation by its own launch — so
       double-clicking utt opened its window behind whatever was already there, and
       no amount of NSApp.activate() afterwards could take the focus back. The Dock
       icon is a runtime decision in AppDelegate instead, which also handles the
       login-item launch this key used to cover. -->
  <key>LSApplicationCategoryType</key><string>public.app-category.productivity</string>
  <!-- Missing NSMicrophoneUsageDescription is a hard crash, not a prompt. -->
  <key>NSMicrophoneUsageDescription</key>
  <string>utt records your voice so it can transcribe it on-device.</string>
  <key>NSInputMonitoringUsageDescription</key>
  <string>utt watches for your push-to-talk hotkey.</string>
  <!-- utt://start, utt://stop, utt://toggle, utt://cancel — see AppDelegate.
       Callers should use `open -g`: bringing utt to the front would make it the
       app the transcript gets pasted into. -->
  <key>CFBundleURLTypes</key>
  <array>
    <dict>
      <key>CFBundleURLName</key><string>dev.jurrejan.utt</string>
      <key>CFBundleTypeRole</key><string>Editor</string>
      <key>CFBundleURLSchemes</key>
      <array><string>utt</string></array>
    </dict>
  </array>
  <!-- Sparkle. Both are empty until an appcast is actually hosted; UpdaterClient
       refuses to start the updater without a feed, because Sparkle aborts with a
       fatal error when it starts without one. Fill SUPublicEDKey from
       `just sparkle-keys`, then SUFeedURL, then `just appcast` per release. -->
  <key>SUFeedURL</key><string></string>
  <key>SUPublicEDKey</key><string></string>
  <key>SUEnableAutomaticChecks</key><false/>
</dict>
</plist>
