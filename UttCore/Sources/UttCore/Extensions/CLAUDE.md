# Extensions

The manifest model — parsing, `sanitized()`, and the capability set an
extension can actually declare. The system-integration invariants (the log,
crash-loop detection, path handling) live one level up, in
`Utt/Clients/Extensions/CLAUDE.md`; read that before touching how utt talks
*about* an extension.
