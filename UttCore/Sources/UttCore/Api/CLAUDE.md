# Api

Request parsing and response rendering — the part a stranger's bytes actually
reach, tested independently of the listener. The reach, auth, and token
invariants live one level up, in `Utt/Clients/Api/CLAUDE.md`; read that before
touching `ApiRoutes` or `ApiDocs`.
